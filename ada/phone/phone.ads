--------------------------------------------------------------------------------------------------------------  Composant destine aux travaux pratiques de << Programmation Modulaire >> de la                        ----  licence d'informatique de l'universite de Limoges                                                     ----------------------------------------------------------------------------------------------------------------  Copie autorisee pour tous les besoins lies a cet enseignement, interdite dans les autres cas.         ----------------------------------------------------------------------------------------------------------------  Pour tous renseignements et operations de maintenance, prendre contact avec Michel Gauthier           --------------------------------------------------------------------------------------------------------------package PHONE is    pragma ELABORATE_BODY ;    NOT_A_NUMBER : exception ;    OVERFLOW     : exception ;private    type FIXED_STRING ( SIZE : POSITIVE ) is record        USED : INTEGER := INTEGER ' FIRST ;        TEXT : STRING ( 1 .. SIZE ) := ( others => '~' ) ;    end record ;    procedure FIXED_BUILD ( USING : in out FIXED_STRING ; FROM : in STRING ) ;    function FIXED_IMAGE ( FROM : FIXED_STRING ) return STRING ;    procedure FIXED_CHECK ( USING : in FIXED_STRING ) ;    pragma INLINE ( FIXED_BUILD , FIXED_IMAGE , FIXED_CHECK ) ;end PHONE ;